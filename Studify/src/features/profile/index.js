/**
 * features/profile — entrada pública do módulo de perfil.
 * Tela, estilos, avatar, meta semanal, badges e lembrete diário (via reminders).
 */
export { default as ProfileScreen } from './ProfileScreen';
export * from './ProfileScreenStyles';
