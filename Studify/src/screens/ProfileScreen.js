/**
 * Shim de compatibilidade — canônico em `src/features/profile/ProfileScreen.js`.
 * Mantido para não quebrar imports existentes (App.js usa o canônico).
 */
export { default } from '../features/profile/ProfileScreen';
